'use client';

import { useEffect } from 'react';
import { useDispatch } from 'react-redux';
import { fetchContacts } from '@/redux/contacts/operations';

const ContactDetailsPage = ({ children }) => {
  return children;
};

export default ContactDetailsPage;
